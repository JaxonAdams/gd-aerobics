import React from 'react';

import LoginHeader from '../components/LoginHeader';

const Login = () => {
    return (
        <div>
            <LoginHeader />
        </div>
    );
};

export default Login;